# 若不用「订阅规则」UI，也可在主配置 [Rule] 里用 RULE-SET（视 Loon 版本语法为准）
# 以下为示意，请以你当前 Loon 文档为准：

# [Remote Rule]
# https://raw.githubusercontent.com/beckyeeky/Loon-Rules/main/rules/Reject.list, policy=REJECT, tag=Reject-Custom, enabled=true
# https://raw.githubusercontent.com/beckyeeky/Loon-Rules/main/rules/Direct.list, policy=DIRECT, tag=Direct-Custom, enabled=true
# https://raw.githubusercontent.com/beckyeeky/Loon-Rules/main/rules/Proxy.list, policy=PROXY, tag=Proxy-Custom, enabled=true

# 更稳妥：App 内「订阅规则」添加上述 URL，并在列表中调整优先级。
